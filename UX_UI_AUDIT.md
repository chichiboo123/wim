# What's In My 앱 구조 기반 UI/UX 사용자 경험 진단 보고서

## 1) 분석 범위
- 대상: `index.html` + 전역 컨트롤러(`js/app.js`) + 공통 스타일(`css/style.css`) + 모듈별 뷰/로직(`js/modules/*.js`, `css/modules.css`) + 저장/내보내기 유틸(`js/storage.js`, `js/export.js`).
- 관점: 정보구조(IA), 상호작용 일관성, 접근성(a11y), 오류 회복성, 모바일 사용성, 학습/교육 맥락에서의 인지부하.

## 2) 앱 구조 요약

### 2-1. 단일 페이지(SPA-lite) 구조
- 해시 라우팅으로 홈/모듈 전환을 처리하며, 페이지 전환 시 `#app-content`를 문자열 템플릿으로 재렌더링하는 구조.
- 라우팅과 렌더 트리거가 명확해 유지보수 난이도는 낮지만, 모든 화면이 문자열 템플릿 + `onclick` 인라인 이벤트에 의존해 규모가 커질수록 결합도가 증가할 가능성이 있음.

### 2-2. 기능 모듈화
- 8개 테마가 독립 파일로 분리되어 있어 기능 단위 분해는 좋음.
- 반면, 공통 상호작용(추가/수정/삭제/드래그/빈 상태)이 모듈마다 다른 방식으로 구현되어 UX 일관성 편차가 존재.

## 3) UX 관점 핵심 문제점 (우선순위)

## P0 (즉시 개선 권장)

### P0-1. 키보드/보조기기 접근성 부족
- 카드/칩/토글/FAB 다수가 `div`/`span` + `onclick` 패턴으로 구현되어 키보드 포커스, Enter/Space 조작, 스크린리더 역할(role) 전달이 불충분함.
- 결과: 데스크톱 키보드 사용자 및 보조기기 사용자가 핵심 기능 탐색/실행에 제약.

### P0-2. 삭제/편집 주요 액션이 hover 의존
- Bag/Brain/Mind/Relationship 삭제 핸들이 hover 시에만 노출되는 패턴이 중심.
- 터치 환경에서는 hover 개념이 약해 삭제 액션 발견성이 낮고, 조작 실수가 발생하기 쉬움.

### P0-3. 오류 피드백/검증 메시지 부재
- 입력 검증 실패 시 조용히 `return`하는 케이스가 많아 사용자 입장에서는 “왜 안 되는지” 알기 어려움.
- Time 입력(시작/종료/할 일)이나 Word 입력(단어/정의) 등은 실수 가능성이 높은데 인라인 에러/가이드가 없음.

## P1 (중요)

### P1-1. 복원(restore) 데이터 신뢰성 검증 미흡
- JSON 복원 시 스키마 검증 없이 저장하므로 잘못된 구조도 앱 상태로 반영될 수 있음.
- 학급/워크숍 환경(다수 파일 공유)에서는 복원 실패율 및 디버깅 비용이 높아질 위험.

### P1-2. 상태 관리 일관성 저하 (특히 edit 흐름)
- Music/Word 편집은 “기존 항목 삭제 후 입력창 재주입” 방식으로 구현되어 취소/실패 시 데이터 손실 가능성과 인지 혼란이 존재.
- 사용자는 일반적으로 “편집 모드 진입 → 저장/취소”를 기대하므로 현재 흐름은 정신모델과 어긋남.

### P1-3. 정보 구조 대비 내비게이션 보조 부족
- 모듈이 8개로 많고 각기 상호작용 패턴이 다른데, 전역 진행상태/최근 사용/즐겨찾기/검색 같은 탐색 보조가 없음.
- 첫 사용자에게는 홈 카드 그리드 이후 탐색 전략이 약함.

## P2 (개선 가치 높음)

### P2-1. 국제화(i18n) 범위 불균형
- 주요 텍스트는 번역되지만, `title` 속성/일부 하드코딩 표현/시간 포맷 등은 언어별 문맥 완성도가 낮을 수 있음.

### P2-2. 시각 의존 정보 전달
- Time 차트에서 색상으로만 구분되는 정보가 많고, 대비·라벨 밀도에 따라 가독성이 급격히 떨어질 수 있음.

### P2-3. 성능/용량 리스크
- 이미지 썸네일/Bag 이미지가 base64 형태로 localStorage에 누적되면 용량 한계(브라우저별 약 5MB 전후) 도달 가능성이 있음.

## 4) 근거 기반 상세 진단

### 4-1. IA/내비게이션
- 해시 라우팅과 모듈 전환은 직관적이나, 현재 구조는 홈 → 모듈 단방향 중심.
- 모듈 수가 많아질수록 사용자는 "어디까지 했는지" 확인하기 어려움(완료/진행 표시 없음).

### 4-2. 상호작용 패턴 일관성
- 일부 모듈은 Enter 키 흐름이 잘 설계되어 있으나, 일부는 클릭 중심 + 포커스 이동 편차가 큼.
- 드래그 인터랙션이 Bag/Brain/Relationship에서 모두 개별 구현되어 경계값·핸들링 방식이 다르고, UX 미세 차이가 축적됨.

### 4-3. 접근성(a11y)
- 버튼이 아닌 클릭 가능 요소가 많아 semantics 부족.
- 포커스 시각화(`:focus-visible`) 규칙이 사실상 없음.
- 이미지 alt 텍스트 품질이 제한적(빈 alt 또는 맥락 없는 alt).

### 4-4. 오류 처리/회복
- `showToast`는 존재하지만 대부분 성공/실패의 총괄 피드백 용도이며, 필드 단위 원인 설명이 없음.
- 복원 실패 시 단일 에러 토스트만 노출되어 어떤 JSON 필드가 문제인지 파악 불가.

### 4-5. 모바일 UX
- 반응형 브레이크포인트는 잘 구성되어 기본 배치는 유지됨.
- 그러나 헤더 액션(테마칩+언어+복원+도움말)이 좁은 화면에서 밀집되어 오탭 가능성이 큼.
- hover 기반 affordance는 모바일에서 발견성이 감소.

## 5) 사용자 여정별 페인포인트

### 신규 사용자(첫 3분)
1. 카드 클릭으로 진입은 쉬움.
2. 모듈마다 조작 방식이 달라 학습 전환 비용 발생.
3. 입력 실패 시 이유가 안 보여 중도 이탈 가능.

### 반복 사용자(저장 데이터 누적)
1. 편집/삭제는 가능하나 대량 데이터 관리(정렬/검색/필터)가 어려움.
2. 이미지/썸네일 누적으로 저장 용량 리스크 증가.

### 교육자(수업 운영)
1. 결과물 공유(export)는 강점.
2. 다중 기기/파일 복원 실패 시 원인 추적 어려움.
3. 접근성 요구가 있는 학습자 지원이 제한적.

## 6) 개선 로드맵 (실행 우선순위)

### Sprint 1 (빠른 체감 개선)
- 클릭 가능한 `div/span`를 `button` 중심으로 전환 + `aria-label` 부여.
- 삭제 버튼 항상 노출(또는 길게누르기/편집모드)로 모바일 발견성 개선.
- 입력 검증 시 인라인 오류 문구 + 실패 토스트 구체화.

### Sprint 2 (신뢰성/학습성 개선)
- JSON 복원 스키마 검증(Zod/수동 validator) 및 모듈별 복구 전략 추가.
- Music/Word 편집을 "편집 상태" 모델(저장/취소)로 재설계.
- 홈에 "최근 작업 모듈", "작성 개수", "진행도" 표시.

### Sprint 3 (확장성/품질)
- 공통 컴포넌트/핸들러 추출(리스트 아이템, 드래그, 폼 검증).
- 이미지 저장 전략 개선(압축/썸네일화/IndexedDB 전환).
- 접근성 점검 자동화(axe-core)와 회귀 체크리스트 도입.

## 7) 요약 결론
- 현재 앱은 교육용 자기탐색 도구로서 "즉시 시작"과 "시각적 친화성"은 강점이다.
- 하지만 실제 사용자 경험 품질을 가르는 요소(접근성, 오류 안내, 편집 안정성, 모바일 발견성)가 구조적으로 약하다.
- 따라서 **P0 접근성 + 피드백 강화**, **P1 데이터 신뢰성/편집 흐름 개선**을 우선 실행하면, 학습자 만족도와 재사용률을 단기간에 크게 높일 수 있다.
